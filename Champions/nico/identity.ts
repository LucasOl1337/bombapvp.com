import { CHAMPION_MEMBERSHIP } from "../membership";

const identity = CHAMPION_MEMBERSHIP.nico;
export const NICO_CHARACTER_ID = identity.characterId;
export const NICO_SKILL_ID = identity.skillId;
export const NICO_SKILL_COOLDOWN_MS = identity.skillCooldownMs;
