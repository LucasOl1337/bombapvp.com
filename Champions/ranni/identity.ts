import { CHAMPION_MEMBERSHIP } from "../membership";

const identity = CHAMPION_MEMBERSHIP.ranni;
export const RANNI_CHARACTER_ID = identity.characterId;
export const RANNI_SKILL_ID = identity.skillId;
export const RANNI_SKILL_COOLDOWN_MS = identity.skillCooldownMs;
