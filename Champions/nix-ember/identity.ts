import { CHAMPION_MEMBERSHIP } from "../membership";

const identity = CHAMPION_MEMBERSHIP["nix-ember"];
export const NIX_EMBER_CHARACTER_ID = identity.characterId;
export const NIX_EMBER_SKILL_ID = identity.skillId;
export const NIX_EMBER_SKILL_COOLDOWN_MS = identity.skillCooldownMs;
